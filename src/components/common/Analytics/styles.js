const styles = {
  projectHistoryContainer:
		'w-[850px] [&>*]:w-full md:w-full rounded-xl bg-mainColor px-6 py-3 flex items-center justify-between [&:not(:last-child)]:mb-3',
  statSquareContainer:
		'p-3 md:p-6 bg-mainColor inline-block rounded-xl relative -z-20',
}

export default styles
